{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "http": "^0.0.1-security"
  },
  "scripts": {
    "dev": "npx nodemon server.js",
    "build": "npx parcel build index.html",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
